version: '3.8'
services:
  backend:
    build: ./backend
    container_name: inventory-app
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - JWT_SECRET=change-this-in-production
    restart: unless-stopped